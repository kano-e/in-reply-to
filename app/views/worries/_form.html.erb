<%= form_for(worry) do |f| %>
  <% if worry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(worry.errors.count, "error") %> prohibited this worry from being saved:</h2>

      <ul>
      <% worry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :detail %>
  </div>

  <div class="actions">
    <%= f.submit 'Post' %>
  </div>
<% end %>
